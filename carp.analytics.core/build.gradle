group = 'dk.cachet.carp.analytics'

version = clientsVersion

publishing {
    publications {
        all {
            pom {
                name = 'Analytics Domain Model and Application Services'
                description =
                    'A domain model and application services to define, plan, and execute analytics workflows over data collected in CARP studies.'
            }
        }
    }
}
